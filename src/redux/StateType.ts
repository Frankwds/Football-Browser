import {SearchData} from "./searchFilter/searchFilterTypes"; 

//A type interface for the only implemented query, scalable with more reducers.
export interface StateType{
    searchQueries: SearchData,
}


